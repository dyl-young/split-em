import baseConfig from "@no-stack/eslint-config/base";
import reactConfig from "@no-stack/eslint-config/react";

/** @type {import('typescript-eslint').Config} */
export default [
  {
    ignores: [".expo/**", "expo-plugins/**"],
  },
  ...baseConfig,
  ...reactConfig,
];
